name: 'Deploy action'
description: 'Deploy custom action'
runs:
  using: 'composite'
  steps:
    - name: Virtual deployment
      shell: bash
      run: echo "Start deployment..."
    - name: Next one
      if: success()
      shell: bash
      run: echo "Done.. yes"